<UserControl x:Class="SherioAPP.userControllers.HotelFrame"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             MinHeight="200">

    <Border Margin="15,8"
            CornerRadius="16"
            Background="White"
            BorderBrush="#F3F4F6"
            BorderThickness="1">

        <Border.Effect>
            <DropShadowEffect BlurRadius="15"
                              ShadowDepth="2"
                              Opacity="0.08"
                              Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="180"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    CornerRadius="16,0,0,16"
                    ClipToBounds="True">
                <Grid>
                    <Image Source="{Binding HotelImageLink}"
       Stretch="UniformToFill"/>
                    <Border Background="Black" Opacity="0.05"/>
                </Grid>
            </Border>

            <StackPanel Grid.Column="1" Margin="20,15">

                <TextBlock Text="{Binding Name}"
                           FontSize="22"
                           FontWeight="ExtraBold"
                           Foreground="#1F2937"
                           TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <materialDesign:PackIcon Kind="MapMarkerOutline" Foreground="#3B82F6" VerticalAlignment="Center" Width="16" Height="16"/>
                    <TextBlock Text="{Binding City.CityName}"
                               FontSize="14"
                               FontWeight="Medium"
                               Foreground="#3B82F6"
                               Margin="5,0,0,0"/>
                </StackPanel>

                <Separator Margin="0,15" Background="#F3F4F6" Height="1"/>

                <StackPanel Orientation="Horizontal" Margin="0,4">
                    <materialDesign:PackIcon Kind="PhoneOutline" Foreground="#6B7280" Width="14" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding PhoneNumber}"
                               FontSize="13"
                               Foreground="#6B7280"
                               Margin="8,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,4">
                    <materialDesign:PackIcon Kind="EmailOutline" Foreground="#6B7280" Width="14" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding Email}"
                               FontSize="13"
                               Foreground="#6B7280"
                               Margin="8,0,0,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,4">
                    <materialDesign:PackIcon Kind="Web" Foreground="#3B82F6" Width="14" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding WebSite}" 
               FontSize="13" 
               Foreground="#3B82F6" 
               Margin="8,0,0,0" 
               TextDecorations="Underline"
               VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>

            <Border Grid.Column="2" 
                    Background="#F9FAFB" 
                    CornerRadius="0,16,16,0"
                    Padding="15">
                <StackPanel VerticalAlignment="Bottom">
                    <Button Content="בדוק זמינות"
        Height="45"
        Background="#2563EB"
        Foreground="White"
        FontWeight="Bold"
        FontSize="14"
        BorderThickness="0"
        Style="{StaticResource MaterialDesignRaisedButton}"
        Click="CheckAvailability_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>