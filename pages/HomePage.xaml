<Page x:Class="SherioAPP.pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      FlowDirection="RightToLeft"
      FontFamily="Segoe UI"
      Title="HomePage"
      d:DesignHeight="500" d:DesignWidth="800">

    <Page.Resources>
        <Style x:Key="InputButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="60" Background="White" Panel.ZIndex="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="10,0">
                <Button x:Name="LoginBtn" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="Login_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Login" Foreground="#2563EB" Width="20" Height="20" Margin="0,0,8,0"/>
                        <TextBlock Text="התחברות" FontSize="14" FontWeight="SemiBold" Foreground="#2563EB"/>
                    </StackPanel>
                </Button>

                <Button x:Name="ProfileBtn" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="Profile_Click" Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AccountCircle" Foreground="#2563EB" Width="20" Height="20" Margin="0,0,8,0"/>
                        <TextBlock x:Name="UserNameText" Text="הפרופיל שלי" FontSize="14" FontWeight="SemiBold" Foreground="#2563EB"/>
                    </StackPanel>
                </Button>

                <Button x:Name="OwnerBtn"
        Background="Transparent"
        BorderThickness="0"
        Cursor="Hand"
        Click="Owner_Click"
        Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Hotel" Foreground="#2563EB" Width="20" Height="20" Margin="0,0,8,0"/>
                        <TextBlock Text="ניהול מלון" FontSize="14" FontWeight="SemiBold" Foreground="#2563EB"/>
                    </StackPanel>
                </Button>

            </StackPanel>

            <TextBlock Grid.Column="1" Text="SHERIO" FontSize="26" FontWeight="ExtraBold" Foreground="#3B82F6" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
            <StackPanel>
                <Grid Height="450">
                    <Image Source="https://images.unsplash.com/photo-1590523277543-a94d2e4eb00b?q=80&amp;w=2832&amp;auto=format&amp;fit=crop" Stretch="UniformToFill"/>
                    <Border Background="Black" Opacity="0.3"/>

                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="החופשה הבאה שלך מתחילה כאן" Foreground="White" FontSize="32" 
                                   FontWeight="Bold" Margin="0,0,0,30" HorizontalAlignment="Center">
                            <TextBlock.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.7"/>
                            </TextBlock.Effect>
                        </TextBlock>

                        <Border Background="White" Width="750" Height="90" CornerRadius="15">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.2"/>
                            </Border.Effect>
                            <Grid Margin="10,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Button x:Name="CityBtn" Grid.Column="0" Click="SelectCity_Click" Style="{StaticResource InputButtonStyle}" Margin="5">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="MapMarker" Foreground="#3B82F6" VerticalAlignment="Center"/>
                                        <StackPanel Margin="10,0">
                                            <TextBlock Text="יעד" FontSize="10" Foreground="Gray"/>
                                            <TextBlock x:Name="SelectedCityText" Text="בחר עיר" FontSize="13" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Button>

                                <Button x:Name="DatesBtn" Grid.Column="1" Click="Dates_Click" Style="{StaticResource InputButtonStyle}" Margin="5">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="CalendarRange" Foreground="#3B82F6" VerticalAlignment="Center"/>
                                        <StackPanel Margin="10,0">
                                            <TextBlock Text="תאריכים" FontSize="10" Foreground="Gray"/>
                                            <TextBlock x:Name="DatesText" Text="מתי מגיעים?" FontSize="13" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Button>

                                <Button x:Name="GuestsBtn" Grid.Column="2" Click="Guests_Click" Style="{StaticResource InputButtonStyle}" Margin="5">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="AccountGroup" Foreground="#3B82F6" VerticalAlignment="Center"/>
                                        <StackPanel Margin="10,0">
                                            <TextBlock Text="אורחים" FontSize="10" Foreground="Gray"/>
                                            <TextBlock x:Name="GuestsLabelText" Text="2 מבוגרים, 0 ילדים" FontSize="12" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Button>

                                <Button Grid.Column="3" Content="חפש" Click="Search_Click" Height="50" Background="#3B82F6" Foreground="White" FontSize="16" FontWeight="Bold" BorderThickness="0" Style="{StaticResource MaterialDesignRaisedButton}" Margin="5">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>

            </StackPanel>
        </ScrollViewer>

        <Popup x:Name="CityPopup" PlacementTarget="{Binding ElementName=CityBtn}" Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
            <Border Background="White" CornerRadius="8" Padding="5" BorderBrush="#EEE" BorderThickness="1">
                <ListBox x:Name="CitiesList" Width="200" SelectionMode="Multiple" DisplayMemberPath="CityName" MaxHeight="250" SelectionChanged="CitySelected" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Hidden"/>
            </Border>
        </Popup>

        <Popup x:Name="DatesPopup" PlacementTarget="{Binding ElementName=DatesBtn}" Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
            <Border Background="White" CornerRadius="12" Padding="20" BorderBrush="#EEE" BorderThickness="1">
                <StackPanel Width="250">
                    <TextBlock Text="בחירת תאריכים" FontWeight="Bold" Margin="0,0,0,15"/>
                    <DatePicker x:Name="CheckInPicker" materialDesign:HintAssist.Hint="תאריך כניסה" Margin="0,5"/>
                    <DatePicker x:Name="CheckOutPicker" materialDesign:HintAssist.Hint="תאריך יציאה" Margin="0,5"/>
                    <Button Content="עדכן תאריכים" Click="ConfirmDates_Click" Margin="0,15,0,0" Background="#3B82F6" BorderThickness="0" Foreground="White"/>
                </StackPanel>
            </Border>
        </Popup>

        <Popup x:Name="GuestsPopup" PlacementTarget="{Binding ElementName=GuestsBtn}" Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
            <Border Background="White" CornerRadius="12" Padding="20" BorderBrush="#E5E7EB" BorderThickness="1">
                <StackPanel Width="260">
                    <TextBlock Text="אורחים" FontWeight="Bold" FontSize="14" Margin="0,0,0,16"/>
                    <Grid Margin="0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="מבוגרים" VerticalAlignment="Center" FontSize="14"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Click="MinusAdults" Width="32" Height="32" Background="#3B82F6" BorderBrush="#3B82F6" Style="{StaticResource MaterialDesignFloatingActionMiniButton}">
                                <materialDesign:PackIcon Kind="Minus" Foreground="White" Width="18" Height="18"/>
                            </Button>
                            <TextBlock x:Name="AdultsCountText" Text="2" Width="35" TextAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="15"/>
                            <Button Click="PlusAdults" Width="32" Height="32" Background="#3B82F6" BorderBrush="#3B82F6" Style="{StaticResource MaterialDesignFloatingActionMiniButton}">
                                <materialDesign:PackIcon Kind="Plus" Foreground="White" Width="18" Height="18"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                    <Grid Margin="0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="ילדים" VerticalAlignment="Center" FontSize="14"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Click="MinusChildren" Width="32" Height="32" Background="#3B82F6" BorderBrush="#3B82F6" Style="{StaticResource MaterialDesignFloatingActionMiniButton}">
                                <materialDesign:PackIcon Kind="Minus" Foreground="White" Width="18" Height="18"/>
                            </Button>
                            <TextBlock x:Name="ChildrenCountText" Text="0" Width="35" TextAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="15"/>
                            <Button Click="PlusChildren" Width="32" Height="32" Background="#3B82F6" BorderBrush="#3B82F6" Style="{StaticResource MaterialDesignFloatingActionMiniButton}">
                                <materialDesign:PackIcon Kind="Plus" Foreground="White" Width="18" Height="18"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</Page>