<Page x:Class="SherioAPP.pages.AdminPages.UsersADMIN"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      FlowDirection="RightToLeft"
      Background="#F8FAFC"
      Title="UsersADMIN"
      Height="500" 
      Width="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Users Management"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0,0,0,15"/>

        <Border Grid.Row="1"
                Background="White"
                CornerRadius="10"
                Padding="10"
                Margin="0,0,0,15">
            <DockPanel>
                <materialDesign:PackIcon Kind="AccountSearch"
                                         Width="20"
                                         Height="20"
                                         Margin="0,0,10,0"/>
                <TextBox x:Name="SearchBox"
                         TextChanged="SearchBox_TextChanged"
                         BorderThickness="0"
                         Background="Transparent"
                         VerticalContentAlignment="Center"/>
            </DockPanel>
        </Border>
        <Button Content="⬅ חזרה לפאנל"
        Width="150"
        Height="35"
        HorizontalAlignment="Right"
        Margin="0,0,0,10"
        Click="BackToAdmin_Click"/>

        <DataGrid Grid.Row="2"
                  x:Name="UsersGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Full Name"
                                    Binding="{Binding FullName}"
                                    Width="2*"/>
                <DataGridTextColumn Header="Email"
                                    Binding="{Binding Email}"
                                    Width="2*"/>
                <DataGridTextColumn Header="Phone"
                                    Binding="{Binding Phone}"
                                    Width="1.5*"/>
                <DataGridCheckBoxColumn Header="Is Owner"
                                        Binding="{Binding IsOwner}"
                                        Width="1*"/>
                <DataGridTemplateColumn Header="Actions" Width="3*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Toggle Owner"
                                        Style="{StaticResource MaterialDesignFlatButton}"
                                        Click="ToggleOwner_Click"
                                        Margin="5"/>
                                <Button Content="Delete"
                                        Style="{StaticResource MaterialDesignFlatButton}"
                                        Foreground="Red"
                                        Click="DeleteUser_Click"
                                        Margin="5"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>